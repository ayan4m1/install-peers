{
  "name": "install-peers-test",
  "license": "MIT",
  "private": true,
  "version": "1.0.0",
  "description": "install-peers-test",
  "dependencies": {
    "is-positive": "^3.1.0"
  },
  "devDependencies": {
    "install-peers": "{{sourceDirectory}}",
    "yarn": "^1.12.3",
    "pnpm": "^2.18.2"
  },
  "peerDependencies": {
    "is-negative": "^2.1.0"
  }
}
